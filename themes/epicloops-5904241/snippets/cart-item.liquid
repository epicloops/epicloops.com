{% comment %}

  This snippet renders a cart item as a table row in the checkout.

  For more information, see:
   - http://docs.shopify.com/themes/liquid-variables/cart

{% endcomment %}
<tr>
  <td>
    <a href="#" class="close" title="Remove {{ cart-item.product.title }} from your cart" onclick="remove_item({{cart-item.variant.id}}); return false;">&times;</a>

    <h4>
      <a href="{{ item.product.url }}" target="_blank">
        {{ cart-item.title }}
      </a>
    </h4>

    {% for p in cart-item.properties %}
      {% unless p.last == blank %}
        {{ p.first }}: {{ p.last }}
        <br />
      {% endunless %}
    {% endfor %}
  </td>

  <td class="text-right">{{ cart-item.price | money }}</td>

  <td class="text-center">
    <input type="text" class="form-control text-center" size="4" name="updates[]" id="updates_{{ cart-item.variant.id }}" value="{{ cart-item.quantity }}" />
  </td>

  <td class="text-right">{{ cart-item.line_price | money }}</td>
</tr>
